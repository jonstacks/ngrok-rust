[package]
name = "ngrok-nif-validation"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "ngrok_nif"
crate-type = ["cdylib"]

[dependencies]
ngrok = { path = "../../../../../ngrok", features = ["ffi"] }
tokio = { version = "1", features = ["rt-multi-thread"] }
