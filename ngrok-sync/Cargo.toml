[package]
name = "ngrok-sync"
version = "0.1.0"
edition = "2021"

[dependencies]
ngrok = { path = "../ngrok" }
tokio = { version = "1", features = ["full"] }
url = "2"

[dev-dependencies]
ngrok-testing = { path = "../ngrok-testing" }
